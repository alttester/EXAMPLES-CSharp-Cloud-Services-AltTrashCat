name: BrowserStack TrashCat
on:
  push:

env:
  BROWSERSTACK_USERNAME: ${{secrets.BROWSERSTACK_USERNAME}}
  BROWSERSTACK_ACCESS_KEY: ${{secrets.BROWSERSTACK_ACCESS_KEY}}
  BROWSERSTACK_APP_ID_SDK_202: ${{secrets.BROWSERSTACK_APP_ID_SDK_202}}

jobs:
  csharp-tests-job:
    name: "CSharp tests on BrowserStack"
    runs-on: ["ubuntu-latest"]
    steps:
      - name: "Checkout the repository"
        uses: actions/checkout@v2

      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: "7.0"

      - name: Restore dependencies
        run: dotnet restore

      - name: Download BrowserStackLocal2
        run: |
          wget https://www.browserstack.com/browserstack-local/BrowserStackLocal-linux-x64.zip
          unzip BrowserStackLocal-linux-x64.zip
          chmod +x BrowserStackLocal
          mv BrowserStackLocal /usr/local/bin/

      - name: Verify BrowserStackLocal binary
        run: |
          ls -l /usr/local/bin/BrowserStackLocal
          /usr/local/bin/BrowserStackLocal --version || true

      - name: Validate BrowserStack app capability
        run: |
          if [ -z "$BROWSERSTACK_APP_ID_SDK_202" ]; then
            echo "BROWSERSTACK_APP_ID_SDK_202 is empty"
            exit 1
          fi

          if [[ "$BROWSERSTACK_APP_ID_SDK_202" == bs://* ]]; then
            echo "Using app_url format in BROWSERSTACK_APP_ID_SDK_202"
            exit 0
          fi


          echo "Validating custom_id in BrowserStack recent apps"
          RESP=$(curl -sS -u "$BROWSERSTACK_USERNAME:$BROWSERSTACK_ACCESS_KEY" "https://api-cloud.browserstack.com/app-automate/recent_apps")
          if echo "$RESP" | grep -q '"custom_id":"'"$BROWSERSTACK_APP_ID_SDK_202"'"'; then
            echo "Found custom_id: $BROWSERSTACK_APP_ID_SDK_202"
          else
            echo "custom_id '$BROWSERSTACK_APP_ID_SDK_202' not found in this BrowserStack account"
            exit 1
          fi

      - name: "Running tests on BrowserStack"
        env:
          BROWSERSTACK_LOCAL_BINARY: /usr/local/bin/BrowserStackLocal
        run: dotnet test --filter "StartPageTests"
